#
# The Jira Cloud platform REST API
# Jira Cloud platform REST API documentation
# Version: 1001.0.0-SNAPSHOT-6617cb5f5c8ebc57da84ed8a4b40f48d8d36f5a1
# Contact: ecosystem@atlassian.com
# Generated by OpenAPI Generator: https://openapi-generator.tech
#

<#
.SYNOPSIS

No summary available.

.DESCRIPTION

Can contain multiple field values of following types depending on `type` key

.PARAMETER Json

JSON object

.OUTPUTS

Fields<PSCustomObject>
#>
function ConvertFrom-JsonToFields {
    [CmdletBinding()]
    Param (
        [AllowEmptyString()]
        [string]$Json
    )

    Process {
        $match = 0
        $matchType = $null
        $matchInstance = $null

        $JsonData = ConvertFrom-Json -InputObject $Json
        # check if the discriminator value is 'mandatoryField'
        if ($JsonData.PSobject.Properties["type"].value -eq "mandatoryField") {
            # try to match MandatoryFieldValue defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToMandatoryFieldValue $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "MandatoryFieldValue"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "anyOfSchemas" = @("MandatoryFieldValue", "MandatoryFieldValueForADF")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'MandatoryFieldValue' defined in anyOf (Fields) using the discriminator lookup (mandatoryField). Proceeding with the typical anyOf type matching."
            }
        }

        # check if the discriminator value is 'mandatoryFieldForADF'
        if ($JsonData.PSobject.Properties["type"].value -eq "mandatoryFieldForADF") {
            # try to match MandatoryFieldValueForADF defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToMandatoryFieldValueForADF $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "MandatoryFieldValueForADF"
                        return [PSCustomObject]@{
                            "ActualType" = ${matchType}
                            "ActualInstance" = ${matchInstance}
                            "anyOfSchemas" = @("MandatoryFieldValue", "MandatoryFieldValueForADF")
                        }
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf with the discriminator lookup, proceed to the next one
                Write-Debug "Failed to match 'MandatoryFieldValueForADF' defined in anyOf (Fields) using the discriminator lookup (mandatoryFieldForADF). Proceeding with the typical anyOf type matching."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match MandatoryFieldValue defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToMandatoryFieldValue $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "MandatoryFieldValue"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'MandatoryFieldValue' defined in anyOf (Fields). Proceeding to the next one if any."
            }
        }

        if ($match -ne 0) { # no match yet
            # try to match MandatoryFieldValueForADF defined in the anyOf schemas
            try {
                $matchInstance = ConvertFrom-JsonToMandatoryFieldValueForADF $Json

                foreach($property in $matchInstance.PsObject.Properties) {
                    if ($null -ne $property.Value) {
                        $matchType = "MandatoryFieldValueForADF"
                        $match++
                        break
                    }
                }
            } catch {
                # fail to match the schema defined in anyOf, proceed to the next one
                Write-Debug "Failed to match 'MandatoryFieldValueForADF' defined in anyOf (Fields). Proceeding to the next one if any."
            }
        }

        if ($match -eq 1) {
            return [PSCustomObject]@{
                "ActualType" = ${matchType}
                "ActualInstance" = ${matchInstance}
                "anyOfSchemas" = @("MandatoryFieldValue", "MandatoryFieldValueForADF")
            }
        } else {
            throw "Error! The JSON payload doesn't matches any type defined in anyOf schemas ([MandatoryFieldValue, MandatoryFieldValueForADF]). JSON Payload: $($Json)"
        }
    }
}

